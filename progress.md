phase: 4
status: ready_for_testing
title: Deploy
detail: "Fixed 10 issues: C1(order ID collision→max seq+1), C2(timezone Z suffix removed), C3(localStorage try-catch), C4(dead code removed), C5(empty row validation), U1(delete modal shows label), U2(status advance confirmation), U3(disable edit for shipped/completed), U4(clickable rows in orders table), U5(product delete reference protection). Deployed at http://localhost:3000."
